# Linux Voice Input - 配置文件示例
# 复制此文件为 config.toml 并根据需要修改参数

[whisper]
# 保留此配置段以保持兼容性
# 目前使用讯飞云在线识别，以下参数暂时未使用
model_path = "~/.local/share/whisper/ggml-base.bin"
language = "zh"
max_duration = 60
silence_threshold = 0.02

# 静音持续时间（秒）
# 说完话后保持静音多久会自动停止录音
# 建议范围：1.5-5.0 秒
# - 1.5: 快速响应，适合短句子和快速对话
# - 2.5: 平衡选择，适合大多数场景
# - 3.0: 默认值，允许句子间有短暂停顿
# - 5.0: 较慢响应，适合说话时有长停顿的情况
silence_duration = 3.0

[xfyun]
# 讯飞云 API 配置（在线识别使用）
# 从 https://console.xfyun.cn/ 获取
# 也可以通过环境变量设置：
#   export XFYUN_APP_ID="your_app_id"
#   export XFYUN_API_SECRET="your_secret"
#   export XFYUN_API_KEY="your_key"
app_id = ""
api_secret = ""
api_key = ""

[audio]
# 音频采样率（Hz）
# 推荐: 16000 (Whisper 和讯飞云都支持)
sample_rate = 16000

# 声道数
# 推荐: 1 (单声道，节省资源)
channels = 1

# 音频块大小（字节，在线模式使用）
# 讯飞云建议: 1280 (40ms @ 16kHz)
chunk_size = 1280

[output]
# 默认输出方式
# 可选: clipboard | file
default = "clipboard"

# 文件输出路径（当 default = "file" 时使用）
# file_path = "~/voice-output.txt"
